coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    patch:
      default:
        target: 50%
        threshold: 10%
    project:
      default:
        target: 70%
        threshold: 5%

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
