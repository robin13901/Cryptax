---
phase: 09-electron-desktop-app
plan: 06
type: execute
wave: 5
depends_on: ["09-05"]
files_modified:
  - packages/electron/src/main/index.ts
  - vitest.config.ts
autonomous: false

must_haves:
  truths:
    - "The packaged Cryptax desktop app launches, imports CSV data, runs the tax engine, and displays results"
    - "The app behaves identically to the web version — all tabs, charts, filters, and report generation work"
    - "All 944+ existing vitest tests pass (no regressions from the Electron integration)"
    - "The web dev workflow (npm run dev) still works independently of Electron"
    - "The Electron dev workflow (npm run electron:dev) works for development"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Updated vitest config that excludes packages/electron from test runs"
  key_links:
    - from: "vitest.config.ts"
      to: "packages/electron"
      via: "exclude pattern prevents electron source from being picked up as tests"
      pattern: "exclude.*electron"
---

<objective>
Final verification of the complete Electron desktop app — both dev mode and packaged installer. Run the full test suite, perform a smoke test of the installed app's core workflow (import -> enrich -> calculate -> report), and verify that both web and Electron workflows coexist.

Purpose: This is the final quality gate for Phase 9. Verifying that the Electron packaging has not introduced regressions and that the packaged app delivers the same experience as the web version.

Output: Verified, working Electron desktop app. Phase 9 complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-electron-desktop-app/09-RESEARCH.md
@.planning/phases/09-electron-desktop-app/09-05-SUMMARY.md

@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure test suite passes and vitest config excludes Electron package</name>
  <files>vitest.config.ts</files>
  <action>
1. Check if the vitest config needs updating. The current config includes test patterns for `packages/backend/src/**/*.test.ts`, `packages/shared/src/**/*.test.ts`, and `packages/frontend/src/**/*.test.{ts,tsx}`. Since there are no test files in `packages/electron/`, this should be fine.

However, ensure the coverage exclude list includes the electron package to avoid coverage tool trying to instrument Electron-specific code that can't run in Node.js:

Add to the `exclude` array in vitest.config.ts coverage section:
```typescript
exclude: [
  ...existing excludes...,
  '**/packages/electron/**',
]
```

2. Run the full test suite:
```bash
npm run test:run
```

All 944+ tests must pass. If any tests fail that are related to the backend refactor (Plan 01), fix them.

3. Run the TypeScript build to verify everything compiles:
```bash
npm run build
```

4. Verify the root `npm run dev` still works (start and stop quickly):
```bash
# Start the web dev server
npm run dev
# Verify both backend and frontend start
# Stop with Ctrl+C
```

5. If any minor issues are found in the Electron main process code (e.g., uncaught promise rejection on graceful shutdown, console warnings), fix them in the relevant files.

6. Add a window title that includes the app version:
```typescript
// In packages/electron/src/main/index.ts, in createWindow():
const { version } = await import('../../../package.json', { with: { type: 'json' } });
// ... or read from app.getVersion()
mainWindow.setTitle(`Cryptax v${app.getVersion()}`);
```

Only if this doesn't introduce import issues. If JSON import causes problems, skip the version in the title.
  </action>
  <verify>
1. `npm run test:run` — all tests pass
2. `npm run build` — TypeScript compilation succeeds for all packages
3. `npm run dev` — web dev workflow starts correctly
4. `npm run electron:dev` — Electron dev workflow starts correctly
  </verify>
  <done>
- All 944+ tests pass
- TypeScript builds cleanly
- vitest coverage excludes Electron package
- Both web and Electron dev workflows work
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Electron desktop app — dev mode and packaged installer both verified working</what-built>
  <how-to-verify>
**Part A: Dev mode smoke test**
1. Run `npm run electron:dev`
2. Verify the app opens
3. Close it

**Part B: Packaged app full workflow test**
1. Launch the installed Cryptax app (from Start Menu or Desktop shortcut)
2. Set up a password (if fresh install) or log in
3. Navigate to Einstellungen (Settings) — verify the tab loads
4. Navigate to Dashboard — verify it loads (empty data is OK)
5. Navigate to Transaktionen — verify it loads
6. Navigate to Steuerreport — verify it loads
7. OPTIONAL (if you have test CSV files): Import a CSV via the import section in Transaktionen, verify data appears
8. Close the app

**Part C: Web mode still works**
1. Run `npm run dev` from project root
2. Open http://localhost:5174 in browser
3. Verify the web app works
4. Stop the server

**Part D: Test suite**
Confirm all tests passed in Task 1 (no action needed if already verified)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. All vitest tests pass (944+)
2. TypeScript builds cleanly
3. Electron dev mode works
4. Packaged app works (all tabs, no crashes)
5. Web dev mode still works
6. Database in correct location (%APPDATA%/Cryptax/)
</verification>

<success_criteria>
- Zero test regressions
- Electron dev mode launches and works
- Packaged app installs and runs correctly on Windows
- Web dev workflow is unaffected
- Database is in the user's app data directory
- The app looks and behaves identically to the web version
- Phase 9 goal is fully met: "The entire Cryptax application is packaged as a native Electron desktop app"
</success_criteria>

<output>
After completion, create `.planning/phases/09-electron-desktop-app/09-06-SUMMARY.md`
</output>
