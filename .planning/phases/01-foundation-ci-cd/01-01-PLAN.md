---
phase: 01-foundation-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - biome.json
  - .gitignore
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/backend/package.json
  - packages/backend/tsconfig.json
  - packages/backend/src/index.ts
  - packages/frontend/package.json
  - packages/frontend/tsconfig.json
  - packages/frontend/tsconfig.app.json
  - packages/frontend/tsconfig.node.json
  - packages/frontend/vite.config.ts
  - packages/frontend/index.html
  - packages/frontend/src/main.tsx
  - packages/frontend/src/vite-env.d.ts
autonomous: true

must_haves:
  truths:
    - "Running `npm install` at the repo root installs dependencies for all three packages"
    - "TypeScript compilation succeeds with `tsc -b` at the root (no type errors)"
    - "Running `biome check .` at root finds zero errors in scaffold files"
    - "Each package can import from @cryptax/shared without path errors"
  artifacts:
    - path: "package.json"
      provides: "Root workspace config with npm workspaces"
      contains: "workspaces"
    - path: "tsconfig.json"
      provides: "Root TypeScript project references"
      contains: "references"
    - path: "biome.json"
      provides: "Lint + format config"
      contains: "biomejs.dev"
    - path: "packages/shared/package.json"
      provides: "@cryptax/shared package definition"
      contains: "@cryptax/shared"
    - path: "packages/backend/package.json"
      provides: "@cryptax/backend package definition"
      contains: "@cryptax/backend"
    - path: "packages/frontend/package.json"
      provides: "@cryptax/frontend package definition"
      contains: "@cryptax/frontend"
  key_links:
    - from: "packages/backend/package.json"
      to: "packages/shared"
      via: "workspace dependency"
      pattern: "@cryptax/shared"
    - from: "packages/frontend/package.json"
      to: "packages/shared"
      via: "workspace dependency"
      pattern: "@cryptax/shared"
---

<objective>
Scaffold the npm workspaces monorepo with three packages (shared, backend, frontend), TypeScript project references, and Biome linting/formatting.

Purpose: Every subsequent plan in Phase 1 (and all future phases) depends on this monorepo structure existing. This is the absolute root dependency.
Output: Working monorepo where `npm install` succeeds, `tsc -b` compiles, and `biome check .` passes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ci-cd/01-RESEARCH.md
@.planning/phases/01-foundation-ci-cd/01-CONTEXT.md

Existing dashboard/ code to reference for porting:
@dashboard/package.json — dependencies to carry forward (react, react-dom, recharts, motion, vite, typescript)
@dashboard/src/index.css — CSS variables and dark theme to port
@dashboard/src/main.tsx — React entry point to port
@dashboard/index.html — HTML template to port
@dashboard/vite.config.ts — Vite config to reference
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root and package scaffolds</name>
  <files>
    package.json
    tsconfig.json
    biome.json
    .gitignore
    packages/shared/package.json
    packages/shared/tsconfig.json
    packages/shared/src/index.ts
    packages/backend/package.json
    packages/backend/tsconfig.json
    packages/backend/src/index.ts
    packages/frontend/package.json
    packages/frontend/tsconfig.json
    packages/frontend/tsconfig.app.json
    packages/frontend/tsconfig.node.json
    packages/frontend/vite.config.ts
    packages/frontend/index.html
    packages/frontend/src/main.tsx
    packages/frontend/src/index.css
    packages/frontend/src/App.tsx
    packages/frontend/src/App.css
    packages/frontend/src/vite-env.d.ts
    packages/frontend/public/vite.svg
  </files>
  <action>
    Create the full monorepo structure from scratch. Do NOT migrate `dashboard/` — build clean, port useful parts.

    **Root package.json:**
    - `"name": "cryptax"`, `"private": true`, `"type": "module"`
    - `"workspaces": ["packages/*"]`
    - Scripts: `"dev"` (concurrently runs backend + frontend), `"build"` (npm run build --workspaces), `"test"` (vitest — placeholder, set up in 01-06), `"lint"` (biome check .), `"format"` (biome format --write .), `"db:generate"` (drizzle-kit generate), `"db:migrate"` (drizzle-kit migrate)
    - devDependencies: `concurrently`, `@biomejs/biome`, `typescript` (~5.9.3)

    **Root tsconfig.json:**
    - `"compilerOptions": { "strict": true, "target": "ES2022" }`
    - `"references"` pointing to all three packages
    - `"files": []` (root doesn't compile anything directly)

    **biome.json** (at root):
    - Schema: `https://biomejs.dev/schemas/2.0.0/schema.json`
    - `"root": true`
    - VCS: enabled, git, useIgnoreFile true
    - Formatter: space indent, width 2, lineWidth 100
    - Linter: recommended true, `noExplicitAny: "error"`, `noForEach: "warn"`
    - JavaScript formatter: single quotes, trailing commas es5, semicolons always
    - Files ignore: `**/dist/**`, `**/node_modules/**`, `**/drizzle/**`, `coverage/**`, `dashboard/**`

    **.gitignore** — update existing to include:
    - `node_modules/`, `dist/`, `coverage/`, `*.db`, `*.db-wal`, `*.db-shm`, `.env`, `.env.local`
    - Keep existing `.planning/intel/` line

    **packages/shared:**
    - package.json: `"name": "@cryptax/shared"`, `"type": "module"`, `"main": "./src/index.ts"`, `"types": "./src/index.ts"`, `"exports": { ".": "./src/index.ts" }`
    - tsconfig.json: extends root, `composite: true`, `module: "ESNext"`, `moduleResolution: "Bundler"`, outDir dist, rootDir src
    - src/index.ts: empty barrel file with a comment `// @cryptax/shared — domain types and utilities`

    **packages/backend:**
    - package.json: `"name": "@cryptax/backend"`, `"type": "module"`, depends on `"@cryptax/shared": "*"`
    - Scripts: `"dev": "tsx watch src/index.ts"`, `"build": "tsc -b"`
    - Dependencies will be added in Plan 01-02 (hono, etc.)
    - devDependencies: `tsx`, `@types/node`
    - tsconfig.json: extends root, `composite: true`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`, references shared
    - src/index.ts: placeholder `console.log('backend starting...')`

    **packages/frontend:**
    - package.json: `"name": "@cryptax/frontend"`, `"type": "module"`, depends on `"@cryptax/shared": "*"`
    - Dependencies: `react` (^19.2.4), `react-dom` (^19.2.4), `recharts` (^3.8.0), `motion` (^12.36.0)
    - devDependencies: `@vitejs/plugin-react` (^6.0.0), `@types/react`, `@types/react-dom`
    - Scripts: `"dev": "vite"`, `"build": "tsc -b && vite build"`, `"preview": "vite preview"`
    - tsconfig.json: just references tsconfig.app.json and tsconfig.node.json (same pattern as existing dashboard)
    - tsconfig.app.json: `module: "ESNext"`, `moduleResolution: "Bundler"`, jsx: react-jsx, composite: true, references shared
    - tsconfig.node.json: for vite.config.ts, `module: "ESNext"`, `moduleResolution: "Bundler"`, composite: true
    - vite.config.ts: react plugin, server port 5174 (proxy added in 01-02)
    - index.html: port from dashboard/index.html (lang="de", title "Cryptax")
    - src/main.tsx: port from dashboard — `createRoot` with `<App />` (no StrictMode, same as current)
    - src/index.css: port from dashboard — CSS variables, dark theme, reset styles
    - src/App.tsx: port the tab structure, nav pills, KPI grid, and empty states from dashboard/src/App.tsx — but import GlassSurface from new location. Use Aurora placeholder div for now (replaced in 01-05).
    - src/App.css: port from dashboard/src/App.css — all existing styles
    - src/vite-env.d.ts: `/// <reference types="vite/client" />`
    - Copy dashboard/public/vite.svg to packages/frontend/public/vite.svg

    Port the GlassSurface component:
    - packages/frontend/src/components/GlassSurface/GlassSurface.tsx — copy from dashboard
    - packages/frontend/src/components/GlassSurface/GlassSurface.css — copy from dashboard

    For the Aurora component, create a minimal placeholder (it will be replaced in 01-05):
    - packages/frontend/src/components/Aurora/Aurora.tsx — simple div with CSS gradient as temporary background
    - packages/frontend/src/components/Aurora/Aurora.css — basic styles

    Run `npm install` at root to create the workspace symlinks and lock file.
  </action>
  <verify>
    1. `npm install` succeeds without errors
    2. `npx tsc -b` compiles with zero errors
    3. `npx biome check .` exits with zero errors
    4. `npm run dev -w packages/frontend` starts Vite on port 5174
    5. Visiting http://localhost:5174 shows the Cryptax UI with tabs and KPI cards
  </verify>
  <done>
    Monorepo with 3 packages exists. npm install works. TypeScript compiles. Biome lint passes. Frontend renders the ported UI with tabs and KPI cards on localhost:5174.
  </done>
</task>

</tasks>

<verification>
- `npm install` at root succeeds
- `npx tsc -b` at root succeeds with zero errors
- `npx biome check .` at root succeeds with zero errors
- `ls packages/shared/src/index.ts packages/backend/src/index.ts packages/frontend/src/App.tsx` — all exist
- `npm run dev -w packages/frontend` serves the app on port 5174
</verification>

<success_criteria>
1. Root package.json has workspaces config pointing to packages/*
2. All three packages have valid package.json with correct names (@cryptax/shared, @cryptax/backend, @cryptax/frontend)
3. TypeScript project references are set up and `tsc -b` compiles all packages
4. Biome config is active and `biome check .` passes on all scaffold files
5. Frontend renders the existing Cryptax UI (tabs, KPI cards, dark theme)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ci-cd/01-01-SUMMARY.md`
</output>
