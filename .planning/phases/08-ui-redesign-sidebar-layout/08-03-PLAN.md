---
phase: 08-ui-redesign-sidebar-layout
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - packages/frontend/src/components/Dashboard/Dashboard.css
  - packages/frontend/src/components/Report/ReportTab.css
  - packages/frontend/src/components/Settings/SettingsTab.css
  - packages/frontend/src/components/Dashboard/Dashboard.test.tsx
  - packages/frontend/src/components/Report/ReportTab.test.tsx
  - packages/frontend/src/components/Settings/SettingsTab.test.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard content fills available width without a max-width cap"
    - "Report tab content fills available width without a max-width cap"
    - "Settings tab has a reasonable max-width for form readability but higher than 860px"
    - "All existing frontend tests pass after CSS adjustments"
    - "Content looks correct at both 1440px and 2560px viewport widths"
  artifacts:
    - path: "packages/frontend/src/components/Dashboard/Dashboard.css"
      provides: "Dashboard layout without max-width: 1800px cap"
    - path: "packages/frontend/src/components/Report/ReportTab.css"
      provides: "Report layout without max-width: 1800px cap"
    - path: "packages/frontend/src/components/Settings/SettingsTab.css"
      provides: "Settings layout with increased max-width"
  key_links:
    - from: "Dashboard.css .dashboard"
      to: "App.css .content-main"
      via: "Dashboard fills content-main width"
      pattern: "width.*100%"
    - from: "ReportTab.css .report-tab"
      to: "App.css .content-main"
      via: "Report fills content-main width"
      pattern: "width.*100%"
---

<objective>
Adjust page container CSS to remove max-width caps so content fills the sidebar layout properly. Fix any test assertions that reference removed CSS classes or DOM structure.

Purpose: With the sidebar eating 240px on the left, the old max-width: 1800px caps waste screen real estate on widescreen monitors. Pages should fill their available space.
Output: Updated CSS for Dashboard, ReportTab, SettingsTab. All tests passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-ui-redesign-sidebar-layout/08-02-SUMMARY.md

@packages/frontend/src/App.tsx
@packages/frontend/src/App.css
@packages/frontend/src/components/Dashboard/Dashboard.css
@packages/frontend/src/components/Report/ReportTab.css
@packages/frontend/src/components/Settings/SettingsTab.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove max-width caps from page containers</name>
  <files>
    packages/frontend/src/components/Dashboard/Dashboard.css
    packages/frontend/src/components/Report/ReportTab.css
    packages/frontend/src/components/Settings/SettingsTab.css
  </files>
  <action>
**Dashboard.css:**
Change `.dashboard`:
```css
.dashboard {
  /* REMOVE: max-width: 1800px; */
  /* REMOVE: margin: 0 auto; */
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}
```
Everything else in Dashboard.css stays unchanged (chart grid, year-selector, freigrenze, empty-state, media queries).

**ReportTab.css:**
Change `.report-tab`:
```css
.report-tab {
  /* REMOVE: max-width: 1800px; */
  /* REMOVE: margin: 0 auto; */
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}
```
Everything else in ReportTab.css stays unchanged.

**SettingsTab.css:**
Change `.settings-tab`:
```css
.settings-tab {
  display: flex;
  flex-direction: column;
  gap: 2rem;
  max-width: 1000px;  /* was 860px — wider now that sidebar eats 240px */
  /* keep margin: 0 auto for settings — forms shouldn't stretch to 2560px */
  margin: 0 auto;
  width: 100%;
}
```
Everything else in SettingsTab.css stays unchanged.

These are surgical changes — only the max-width and margin properties. No other style rules are modified.
  </action>
  <verify>
Visually confirm: `.dashboard` and `.report-tab` no longer have `max-width`. `.settings-tab` has `max-width: 1000px`.
  </verify>
  <done>
Dashboard and Report containers fill available width. Settings has a wider but still capped max-width for form readability. All other styles preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix tests for layout changes and run full test suite</name>
  <files>
    packages/frontend/src/components/Dashboard/Dashboard.test.tsx
    packages/frontend/src/components/Report/ReportTab.test.tsx
    packages/frontend/src/components/Settings/SettingsTab.test.tsx
  </files>
  <action>
Run the full frontend test suite first: `cd C:/SAPDevelop/Privat/Cryptax && npx vitest run packages/frontend/`

If any tests fail due to the App.tsx structural changes (e.g., tests querying for removed `.nav-pills`, `.header h1`, or pill buttons), fix them:

**Potential failure points to check:**

1. **Dashboard.test.tsx** — These tests render `<Dashboard>` directly (not through App), so they should be unaffected by App.tsx changes. If any test references removed DOM elements, update the selector.

2. **ReportTab.test.tsx** — Same: renders `<ReportTab>` directly. Should be unaffected.

3. **SettingsTab.test.tsx** — Same: renders `<SettingsTab>` directly. Should be unaffected.

4. **Any test that renders `<App>`** — Check if there's an App.test.tsx. If it exists and queries for `.nav-pills` or pill buttons, update it to query for Sidebar nav items instead.

5. **E2E tests (e2e/report.spec.ts)** — Check if E2E tests reference `.nav-pill` or pill button selectors. If so, update to use sidebar nav buttons. But do NOT run E2E tests (they require a running server) — just fix selectors if needed.

**If all tests pass on first run:** No changes needed to test files. Just confirm the count and document it.

**If tests fail:** Analyze each failure, fix the assertion or selector, re-run until all pass. Max 10 iterations (Ralph Loop).

After all frontend tests pass, run the full monorepo test suite to confirm nothing is broken:
`cd C:/SAPDevelop/Privat/Cryptax && npm test`
  </action>
  <verify>
`cd C:/SAPDevelop/Privat/Cryptax && npx vitest run packages/frontend/` — all tests pass.
`cd C:/SAPDevelop/Privat/Cryptax && npm test` — full suite passes (930+ tests).
  </verify>
  <done>
All existing frontend tests pass. Full monorepo test suite passes. No regressions from the layout restructure.
  </done>
</task>

</tasks>

<verification>
- `npm test` passes full suite (930+ tests)
- Dashboard.css: no max-width cap
- ReportTab.css: no max-width cap
- SettingsTab.css: max-width increased from 860px to 1000px
- No CSS references to removed classes (.nav-pills, .header, .tab-bar-container)
</verification>

<success_criteria>
- Dashboard and Report content fills available width beside sidebar
- Settings content has wider max-width (1000px) but stays centered
- All 930+ tests pass
- No visual regressions in component styling (only container width changes)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-redesign-sidebar-layout/08-03-SUMMARY.md`
</output>
