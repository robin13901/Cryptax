---
phase: 08-ui-redesign-sidebar-layout
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Sidebar navigation looks professional with glass-frosted dark aesthetic"
    - "Active tab has visible blue accent indicator"
    - "Sidebar collapse/expand animates smoothly"
    - "Dashboard charts fill available width correctly on widescreen"
    - "FloatingLines background visible through sidebar glass"
    - "App looks correct at desktop, tablet, and narrow viewport widths"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete sidebar layout redesign.

Purpose: CSS/layout changes require human eye confirmation. Screenshots and automated tests cannot fully verify aesthetic quality, spacing, animation smoothness, and overall professional feel.
Output: User approval or list of visual issues to fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ui-redesign-sidebar-layout/08-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete sidebar navigation layout replacing the top pill tabs:
- New glass-frosted sidebar with 4 nav items (Dashboard, Transaktionen, Steuerreport, Einstellungen)
- Collapsible sidebar (240px expanded, 64px icon-only collapsed)
- Content area fills remaining width
- Page title header in content area
- FloatingLines background visible through sidebar glass
- All existing functionality preserved (tab switching, auth, import, year selection)
  </what-built>
  <how-to-verify>
Start the dev server: `cd C:/SAPDevelop/Privat/Cryptax && npm run dev`

1. **Login screen:** Navigate to http://localhost:5173. Verify the login screen renders centered (no sidebar visible).

2. **Sidebar appearance:** After logging in, verify:
   - Left sidebar has glass/frosted dark background
   - "C" logo and "Cryptax" brand text at top
   - 4 nav items with SVG icons and text labels
   - Active tab has blue left-border accent
   - FloatingLines animation visible through sidebar glass

3. **Navigation:** Click each nav item:
   - Dashboard: shows charts and KPIs (fill available width)
   - Transaktionen: shows transaction list with import toggle
   - Steuerreport: shows report preview with year selector
   - Einstellungen: shows exchange connections + password change

4. **Collapse/expand:** Click the collapse toggle button at sidebar bottom:
   - Sidebar animates smoothly to 64px (icon-only)
   - Content area expands smoothly to fill
   - Hovering over icon shows tooltip with label
   - Click toggle again: sidebar expands back to 240px
   - Reload page: collapse state is remembered

5. **Responsive:** Resize browser window to narrow width (~768px or less):
   - Sidebar auto-collapses to icon-only mode
   - Content area adjusts
   - Page remains usable

6. **Widescreen:** If available, stretch window to 2560px+ width:
   - Dashboard charts spread across full width (no 1800px cap)
   - No awkward empty space
  </how-to-verify>
  <resume-signal>Type "approved" if the layout looks professional and correct, or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Human confirms visual quality of the redesigned layout.
</verification>

<success_criteria>
User approves the sidebar layout as professional-looking and correctly functioning.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-redesign-sidebar-layout/08-04-SUMMARY.md`
</output>
