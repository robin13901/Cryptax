---
phase: 07-exchange-api-security
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/backend/src/auth/security-audit.test.ts
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "No credential strings appear in any console.log, console.error, or logger() output"
    - ".env and credential-adjacent files are gitignored"
    - "Test suite verifies that auth modules never log sensitive data"
  artifacts:
    - path: "packages/backend/src/auth/security-audit.test.ts"
      provides: "Security audit tests: no credential leakage in logs, env vars, responses"
    - path: ".gitignore"
      provides: "Updated with credential-adjacent file patterns"
  key_links:
    - from: "packages/backend/src/auth/security-audit.test.ts"
      to: "packages/backend/src/auth/crypto.ts"
      via: "import and test for no side-effect logging"
      pattern: "import.*crypto"
---

<objective>
Harden security by auditing all log statements for credential leakage, updating .gitignore, and creating security-focused tests that catch accidental credential exposure.

Purpose: SECU-04 requires no credentials in logs, console output, error messages, or git-tracked files. This plan creates automated guards that catch violations early and documents the security boundary.

Output: Security audit test suite, updated .gitignore, verified clean log output.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-exchange-api-security/07-RESEARCH.md
@.planning/phases/07-exchange-api-security/07-01-SUMMARY.md

Key codebase files:
@packages/backend/src/index.ts
@packages/backend/src/auth/crypto.ts
@packages/backend/src/auth/auth-store.ts
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security audit tests + .gitignore hardening</name>
  <files>
    packages/backend/src/auth/security-audit.test.ts
    .gitignore
  </files>
  <action>
    1. Create `packages/backend/src/auth/security-audit.test.ts` with these security validation tests:

       **Log leakage tests:**
       - Spy on `console.log`, `console.error`, `console.warn` before each test, restore after
       - Test: Calling `hashPassword('my-secret-password')` does not log the password string
       - Test: Calling `verifyPassword('my-secret-password', validHash)` does not log the password
       - Test: Creating an auth route handler and calling login does not log the password in console output
       - For each spy, assert: `spy.mock.calls.flat().join(' ')` does NOT contain the test password string

       **Response leakage tests:**
       - Test: POST /api/auth/login error response does not echo back the submitted password
       - Test: POST /api/auth/setup response does not echo back the submitted password
       - Test: GET /api/auth/status response contains only `hasPassword` and `authenticated` keys (no extra fields)

       **Source code audit tests (static analysis):**
       - Read all .ts files in `packages/backend/src/auth/` directory
       - Test: No file contains `console.log(.*password` pattern (regex check on source)
       - Test: No file contains `console.log(.*secret` pattern
       - Test: No file contains `console.log(.*apiKey` pattern
       - Test: No file contains `console.log(.*masterKey` pattern
       - Use `fs.readFileSync` + `glob` or manually list files to read source code in test

    2. Update root `.gitignore`:
       - Add `.env` (if not already present)
       - Add `.env.local`
       - Add `.env.production`
       - Add `*.key`
       - Add `*.pem`
       - Add `credentials.json`
       - Add `secrets/`
       - Add comment: `# Credential-adjacent files (SECU-04)`

    3. Verify existing source files:
       - Scan `packages/backend/src/index.ts` — the existing `logger()` middleware logs request paths. Verify it does NOT log request bodies (Hono's default logger only logs method+path+status, which is safe).
       - If any route logs `c.req.json()` or request body in error handlers, remove or redact it.
  </action>
  <verify>
    Run `npx vitest run packages/backend/src/auth/security-audit` — all tests pass.
    Run `git check-ignore .env` — returns `.env` (gitignored).
    Grep for `console.log.*password` in packages/backend/src/ — zero matches.
  </verify>
  <done>
    Security audit tests catch credential leakage in logs, responses, and source code. .gitignore updated to exclude credential-adjacent files. No plaintext credentials in any log output path.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/backend/src/auth/security-audit` — all security tests pass
2. `git check-ignore .env .env.local credentials.json` — all gitignored
3. Grep for `console.log.*password|console.log.*secret|console.log.*apiKey` in backend source — zero matches
</verification>

<success_criteria>
- Security audit test suite catches credential leakage in logs, responses, and source
- .gitignore blocks .env, .env.local, *.key, *.pem, credentials.json, secrets/
- No password/secret/apiKey strings appear in any console output
- Hono logger confirmed to not log request bodies
</success_criteria>

<output>
After completion, create `.planning/phases/07-exchange-api-security/07-03-SUMMARY.md`
</output>
