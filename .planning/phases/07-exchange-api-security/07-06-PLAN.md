---
phase: 07-exchange-api-security
plan: 06
type: execute
wave: 3
depends_on: ["07-02", "07-04"]
files_modified:
  - packages/frontend/src/components/Settings/SettingsTab.tsx
  - packages/frontend/src/components/Settings/SettingsTab.css
  - packages/frontend/src/components/Settings/ExchangeCard.tsx
  - packages/frontend/src/components/Settings/ExchangeCard.css
  - packages/frontend/src/components/Settings/CredentialForm.tsx
  - packages/frontend/src/components/Settings/CredentialForm.css
  - packages/frontend/src/components/Settings/PasswordChange.tsx
  - packages/frontend/src/components/Settings/SettingsTab.test.tsx
  - packages/frontend/src/components/Settings/CredentialForm.test.tsx
  - packages/frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Settings tab shows exchange management section at the top"
    - "User can add a new Bitget connection with apiKey, secret, and passphrase"
    - "Credential fields are masked (password type) with eye toggle to reveal"
    - "After saving, auto-test runs and shows green checkmark or red error"
    - "User can delete a connection with confirmation dialog"
    - "Password change section appears below exchange management"
    - "Existing connections listed with label, exchange name, last sync time"
  artifacts:
    - path: "packages/frontend/src/components/Settings/SettingsTab.tsx"
      provides: "Combined settings page: exchanges at top, app settings below"
    - path: "packages/frontend/src/components/Settings/ExchangeCard.tsx"
      provides: "Per-exchange card: label, status, sync button, delete button"
    - path: "packages/frontend/src/components/Settings/CredentialForm.tsx"
      provides: "3-field credential entry form with masked inputs and eye toggle"
    - path: "packages/frontend/src/components/Settings/PasswordChange.tsx"
      provides: "Change password form in app settings section"
  key_links:
    - from: "packages/frontend/src/components/Settings/SettingsTab.tsx"
      to: "/api/exchanges"
      via: "fetch GET for listing, POST for create, DELETE for remove"
      pattern: "fetch.*api/exchanges"
    - from: "packages/frontend/src/components/Settings/CredentialForm.tsx"
      to: "/api/exchanges"
      via: "POST /api/exchanges to save new connection"
      pattern: "fetch.*api/exchanges.*POST"
    - from: "packages/frontend/src/App.tsx"
      to: "packages/frontend/src/components/Settings/SettingsTab.tsx"
      via: "activeTab === 'settings' renders SettingsTab"
      pattern: "SettingsTab"
---

<objective>
Build the exchange management UI: SettingsTab with exchange connections (add/delete/test) and password change section, plus individual ExchangeCard and CredentialForm components.

Purpose: EXCH-03 requires a connection management UI. CONTEXT.md specifies a combined "Einstellungen" tab with exchanges at top and app settings below. This plan delivers the full settings page that lets users manage exchange connections.

Output: Complete Settings tab with exchange card list, credential entry form (3 fields, masked), connection testing, delete with confirmation, and password change. Replaces placeholder from 07-04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-exchange-api-security/07-RESEARCH.md
@.planning/phases/07-exchange-api-security/07-CONTEXT.md
@.planning/phases/07-exchange-api-security/07-02-SUMMARY.md
@.planning/phases/07-exchange-api-security/07-04-SUMMARY.md

Key codebase files:
@packages/frontend/src/App.tsx
@packages/frontend/src/components/GlassSurface/GlassSurface.tsx
@packages/frontend/src/components/Dashboard/Dashboard.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: CredentialForm + ExchangeCard components</name>
  <files>
    packages/frontend/src/components/Settings/CredentialForm.tsx
    packages/frontend/src/components/Settings/CredentialForm.css
    packages/frontend/src/components/Settings/ExchangeCard.tsx
    packages/frontend/src/components/Settings/ExchangeCard.css
    packages/frontend/src/components/Settings/CredentialForm.test.tsx
  </files>
  <action>
    1. Create `packages/frontend/src/components/Settings/CredentialForm.tsx`:
       - Form with 3 fields: "API Key", "Secret", "Passphrase" (labels in English since Bitget uses English)
       - All fields type="password" by default with eye icon toggle button to reveal/hide each individually
       - "Label" text input for user's connection name (e.g., "Mein Bitget Konto")
       - Exchange selector: dropdown defaulting to "Bitget" (only option for now, but extensible)
       - "Verbindung speichern" submit button
       - "Verbindung testen" button (secondary, outline style) — calls POST /api/exchanges/:id/test after save
       - Props: `onSave: (connection: ExchangeConnection) => void`, `onCancel: () => void`
       - State: apiKey, secret, password (passphrase), label, showApiKey/showSecret/showPassword booleans, saving boolean, testResult: { success: boolean, error?: string } | null
       - On submit: POST /api/exchanges with { exchange: 'bitget', label, credentials: { apiKey, secret, password } }
       - After save success: auto-call POST /api/exchanges/:id/test, show green checkmark or red error badge
       - Validation: all 4 fields (label + 3 credentials) must be non-empty

    2. Create `packages/frontend/src/components/Settings/CredentialForm.css`:
       - Form layout with stacked fields, gap between them
       - `.credential-input-group` — label + input + eye toggle in a row
       - `.eye-toggle` — icon button (use unicode eye symbol or SVG) positioned inside input
       - `.test-result` — green (#5fdc8a) checkmark or red (#E50000) error text
       - Consistent with Glassmorphism dark theme

    3. Create `packages/frontend/src/components/Settings/ExchangeCard.tsx`:
       - Uses GlassSurface for card wrapper
       - Displays: exchange name ("Bitget"), label, connection status (connected/not tested)
       - Last sync time: "Letzte Synchronisierung: {date}" or "Noch nicht synchronisiert"
       - Sync button: "Synchronisieren" (placeholder — will be wired in 07-07)
       - Delete button: "Entfernen" — shows confirm dialog before deletion
       - Confirm dialog text: "Verbindung entfernen? Bereits importierte Trades bleiben erhalten." (per CONTEXT.md)
       - Props: `connection: ExchangeConnection`, `onDelete: (id: number) => void`, `onSync: (id: number) => void`

    4. Create `packages/frontend/src/components/Settings/ExchangeCard.css`:
       - Card layout with header (exchange name + label) and footer (action buttons)
       - Status badge: green dot for tested/connected, gray for untested
       - Responsive: full width on mobile, card layout on desktop

    5. Create `packages/frontend/src/components/Settings/CredentialForm.test.tsx`:
       - Test: Renders all 3 credential fields + label field
       - Test: Fields are password-type by default (masked)
       - Test: Eye toggle reveals field content (switches to type="text")
       - Test: Submit disabled when fields empty
       - Test: Calls onSave after successful save (mock fetch)
       - Test: Shows test result after auto-test
  </action>
  <verify>
    Run `npx vitest run packages/frontend/src/components/Settings/` — all tests pass.
  </verify>
  <done>
    CredentialForm with 3 masked credential fields + eye toggle + auto-test on save. ExchangeCard displays connection info with sync and delete buttons. Tests cover form interactions.
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsTab + PasswordChange + App.tsx integration</name>
  <files>
    packages/frontend/src/components/Settings/SettingsTab.tsx
    packages/frontend/src/components/Settings/SettingsTab.css
    packages/frontend/src/components/Settings/PasswordChange.tsx
    packages/frontend/src/components/Settings/SettingsTab.test.tsx
    packages/frontend/src/App.tsx
  </files>
  <action>
    1. Create `packages/frontend/src/components/Settings/SettingsTab.tsx`:
       - Two sections (per CONTEXT.md: exchanges at top, app settings below):

       **Section 1: Boersenverbindungen (Exchange Connections)**
       - Header: "Boersenverbindungen" with "Verbindung hinzufuegen" button
       - "Alle synchronisieren" button at top (per CONTEXT.md) — placeholder handler for now (07-07 wires it)
       - Lists ExchangeCard components for each connection
       - Empty state: "Noch keine Verbindungen eingerichtet. Fuegen Sie eine Exchange-Verbindung hinzu."
       - "Verbindung hinzufuegen" toggles CredentialForm visibility (inline expand or modal)
       - On mount: fetch GET /api/exchanges to load connections
       - On delete: call DELETE /api/exchanges/:id, refetch list
       - On save: refetch list after CredentialForm completes

       **Section 2: App-Einstellungen (App Settings)**
       - Header: "App-Einstellungen"
       - PasswordChange component
       - Logout button: "Abmelden" — calls POST /api/auth/logout, reloads page

    2. Create `packages/frontend/src/components/Settings/SettingsTab.css`:
       - `.settings-tab` — max-width container matching dashboard
       - `.settings-section` — GlassSurface-wrapped section with padding
       - `.settings-section__header` — flex: title + action button
       - `.exchange-list` — vertical stack of ExchangeCards with gap
       - Consistent spacing and typography with existing Dashboard/TransactionList

    3. Create `packages/frontend/src/components/Settings/PasswordChange.tsx`:
       - Simple form: current password + new password + confirm new password
       - Submit: POST /api/auth/change-password (note: this endpoint needs to exist — if 07-01 didn't create it, add it here as a simple route addition or document it as TODO)
       - Actually, since 07-01 only has setup/login/logout/status, add password change logic:
         Verify current password via `verifyPassword`, then update hash via `setPasswordHash(hashPassword(newPassword))`.
         For simplicity, can call POST /api/auth/setup is wrong (409 if already set). Instead, create inline logic:
         POST /api/auth/change-password { currentPassword, newPassword } — verify current, hash new, update. OR keep this as a simple placeholder that says "Passwort aendern — Funktion kommt bald" to avoid scope creep. RECOMMENDED: implement the change-password backend route inline (3 lines in auth.ts: verify old, hash new, update store).

    4. Create `packages/frontend/src/components/Settings/SettingsTab.test.tsx`:
       - Test: Renders "Boersenverbindungen" header
       - Test: Shows empty state when no connections
       - Test: Lists exchange cards when connections exist (mock fetch)
       - Test: "Verbindung hinzufuegen" button shows credential form
       - Test: Delete triggers confirmation dialog

    5. Update `packages/frontend/src/App.tsx`:
       - Replace settings placeholder with `<SettingsTab onLogout={handleLogout} />`
       - Pass handleLogout to SettingsTab (or let SettingsTab handle logout internally)
       - Remove the temporary settings-placeholder div
  </action>
  <verify>
    Run `npx vitest run packages/frontend/src/components/Settings/` — all tests pass.
    Run `npx vitest run` — full suite passes.
  </verify>
  <done>
    SettingsTab shows exchange connections (add/delete) and app settings (password change, logout). ExchangeCard, CredentialForm, PasswordChange all integrated. Settings tab placeholder in App.tsx replaced with real SettingsTab component.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass
2. Settings tab shows two sections: exchange connections and app settings
3. Add connection form has 3 masked fields with eye toggles
4. Delete shows German confirmation dialog
5. Logout button works from settings page
</verification>

<success_criteria>
- SettingsTab renders with exchange management at top, app settings below
- CredentialForm has 3 masked fields (apiKey, secret, passphrase) with eye toggle reveal
- Auto-test on save shows green checkmark or red error
- ExchangeCard shows connection info, sync button (placeholder), delete button with confirm
- Empty state shown when no connections
- "Alle synchronisieren" button present (handler wired in 07-07)
- Password change form functional
- App.tsx settings tab renders real SettingsTab component
- All frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-exchange-api-security/07-06-SUMMARY.md`
</output>
