---
phase: 07-exchange-api-security
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/frontend/src/components/Auth/LoginCard.tsx
  - packages/frontend/src/components/Auth/LoginCard.css
  - packages/frontend/src/components/Auth/SetupCard.tsx
  - packages/frontend/src/components/Auth/SetupCard.css
  - packages/frontend/src/components/Auth/LoginCard.test.tsx
  - packages/frontend/src/components/Auth/SetupCard.test.tsx
  - packages/frontend/src/App.tsx
  - packages/frontend/src/App.css
autonomous: true

must_haves:
  truths:
    - "On first launch (no password set), the SetupCard is shown -- not the app"
    - "After password creation, the LoginCard is shown"
    - "Entering correct password on LoginCard shows the full app"
    - "Entering wrong password shows 'Falsches Passwort' error"
    - "A 4th tab 'Einstellungen' appears in the nav bar when authenticated"
    - "Logout button clears session and returns to LoginCard"
  artifacts:
    - path: "packages/frontend/src/components/Auth/LoginCard.tsx"
      provides: "Centered password login form with error state"
    - path: "packages/frontend/src/components/Auth/SetupCard.tsx"
      provides: "First-run password creation form with confirmation"
    - path: "packages/frontend/src/App.tsx"
      provides: "Auth state machine: loading -> setup -> login -> authenticated"
      contains: "authState"
  key_links:
    - from: "packages/frontend/src/App.tsx"
      to: "/api/auth/status"
      via: "fetch on mount to determine auth state"
      pattern: "fetch.*api/auth/status"
    - from: "packages/frontend/src/components/Auth/LoginCard.tsx"
      to: "/api/auth/login"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/auth/login"
    - from: "packages/frontend/src/components/Auth/SetupCard.tsx"
      to: "/api/auth/setup"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/auth/setup"
---

<objective>
Create the frontend authentication gate: LoginCard, SetupCard, auth state machine in App.tsx, and the new Einstellungen tab placeholder.

Purpose: SECU-01 requires a login screen before accessing the app. This plan implements the full frontend auth flow: first-launch setup, login, authenticated app access, and logout. It also adds the 4th "Einstellungen" tab that the exchange management UI (07-06) will populate.

Output: Functional auth flow in the browser. App shows setup/login/authenticated states correctly. New Settings tab visible in navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-exchange-api-security/07-RESEARCH.md
@.planning/phases/07-exchange-api-security/07-CONTEXT.md
@.planning/phases/07-exchange-api-security/07-01-SUMMARY.md

Key codebase files:
@packages/frontend/src/App.tsx
@packages/frontend/src/App.css
@packages/frontend/src/components/GlassSurface/GlassSurface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoginCard + SetupCard components</name>
  <files>
    packages/frontend/src/components/Auth/LoginCard.tsx
    packages/frontend/src/components/Auth/LoginCard.css
    packages/frontend/src/components/Auth/SetupCard.tsx
    packages/frontend/src/components/Auth/SetupCard.css
    packages/frontend/src/components/Auth/LoginCard.test.tsx
    packages/frontend/src/components/Auth/SetupCard.test.tsx
  </files>
  <action>
    1. Create `packages/frontend/src/components/Auth/LoginCard.tsx`:
       - Centered card using GlassSurface component (matches existing design system)
       - Header: "Cryptax" title + "Anmeldung" subtitle
       - Password input field (type="password")
       - "Anmelden" submit button
       - Error message display (red text below input): shows "Falsches Passwort" on 401
       - Props: `onSuccess: () => void` — called after successful login
       - State: password string, error string | null, loading boolean
       - On submit: POST /api/auth/login with { password }. If 200, call onSuccess(). If 401, set error "Falsches Passwort". If other error, set generic error.
       - Use motion/react for subtle entrance animation (opacity + y translate, matching existing tab animations)

    2. Create `packages/frontend/src/components/Auth/LoginCard.css`:
       - `.login-card` — centered in viewport (flexbox or grid), max-width 400px
       - `.login-card__title` — large, matches header h1 style
       - `.login-card__input` — full width, glassmorphism-style input (semi-transparent bg, border)
       - `.login-card__button` — primary action button (blue #0070F2 background)
       - `.login-card__error` — red text (#E50000), small font
       - Consistent with existing Glassmorphism dark theme

    3. Create `packages/frontend/src/components/Auth/SetupCard.tsx`:
       - Similar centered card layout using GlassSurface
       - Header: "Cryptax" title + "Passwort erstellen" subtitle
       - Two password inputs: "Passwort" and "Passwort bestaetigen"
       - "Passwort setzen" submit button
       - Client-side validation: password >= 8 chars, passwords match
       - Error messages for validation failures and server errors
       - Props: `onSuccess: () => void` — called after successful setup
       - On submit: POST /api/auth/setup with { password }. If 201, call onSuccess(). Handle errors.

    4. Create `packages/frontend/src/components/Auth/SetupCard.css`:
       - Same centered layout as LoginCard
       - `.setup-card__inputs` — stacked password fields with gap
       - Reuse similar styling patterns from LoginCard

    5. Create `packages/frontend/src/components/Auth/LoginCard.test.tsx`:
       - Test: Renders password input and submit button
       - Test: Shows error on failed login (mock fetch to return 401)
       - Test: Calls onSuccess on successful login (mock fetch to return 200)
       - Test: Disables button while loading
       - Use @testing-library/react + @testing-library/user-event

    6. Create `packages/frontend/src/components/Auth/SetupCard.test.tsx`:
       - Test: Renders two password inputs and submit button
       - Test: Shows error when passwords don't match
       - Test: Shows error when password < 8 chars
       - Test: Calls onSuccess on successful setup (mock fetch 201)
  </action>
  <verify>
    Run `npx vitest run packages/frontend/src/components/Auth/` — all tests pass.
  </verify>
  <done>
    LoginCard and SetupCard render correctly with Glassmorphism styling, handle form submission, show errors, and call onSuccess callbacks. Tests cover core interactions.
  </done>
</task>

<task type="auto">
  <name>Task 2: App.tsx auth state machine + Settings tab</name>
  <files>
    packages/frontend/src/App.tsx
    packages/frontend/src/App.css
  </files>
  <action>
    1. Update `packages/frontend/src/App.tsx`:

       - Add auth state machine: `const [authState, setAuthState] = useState<'loading' | 'setup' | 'login' | 'authenticated'>('loading');`

       - On mount (useEffect), call `GET /api/auth/status`:
         - If `hasPassword === false` -> setAuthState('setup')
         - If `hasPassword === true && authenticated === false` -> setAuthState('login')
         - If `hasPassword === true && authenticated === true` -> setAuthState('authenticated')
         - On fetch error (network failure) -> still set 'login' (graceful degradation)

       - Add `handleLogout` function: POST /api/auth/logout, then setAuthState('login')

       - Render based on authState:
         - `'loading'`: Minimal centered spinner/loading text on FloatingLines background
         - `'setup'`: FloatingLines bg + SetupCard (onSuccess -> transition to 'login')
         - `'login'`: FloatingLines bg + LoginCard (onSuccess -> transition to 'authenticated')
         - `'authenticated'`: Full app (existing content)

       - Update TabId type: `type TabId = 'dashboard' | 'transactions' | 'report' | 'settings';`

       - Add 4th tab: `{ id: 'settings', label: 'Einstellungen' }`

       - Add settings tab content (placeholder for now — 07-06 will implement):
         ```tsx
         {activeTab === 'settings' && (
           <motion.div key="settings" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -12 }} transition={{ duration: 0.25, ease: 'easeOut' }}>
             <div className="settings-placeholder">
               <p>Einstellungen — Kommt in Kuerze</p>
               <button type="button" onClick={handleLogout} className="logout-button">
                 Abmelden
               </button>
             </div>
           </motion.div>
         )}
         ```

    2. Update `packages/frontend/src/App.css`:
       - `.auth-loading` — centered loading state (spinner or pulsing text)
       - `.settings-placeholder` — basic centered content
       - `.logout-button` — styled button matching nav-pill style but red/destructive
       - Ensure login/setup pages render nicely with FloatingLines background visible behind

    3. Verify existing tab styling handles 4 tabs gracefully (no overflow or cramping).
  </action>
  <verify>
    Run `npx vitest run packages/frontend/` — all frontend tests pass (may need to update App.test.tsx or Dashboard.test.tsx if they depend on app structure).
    Run `npx vitest run` — full suite passes.
  </verify>
  <done>
    App.tsx has auth state machine (loading -> setup -> login -> authenticated). FloatingLines background visible on all auth states. 4th "Einstellungen" tab added with logout button. Login/Setup flow works end-to-end with backend auth routes.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass
2. Visual check: app shows setup screen on first launch, login after setup, full app after login
3. Tab bar shows 4 tabs including "Einstellungen"
4. Logout returns to login screen
</verification>

<success_criteria>
- Auth state machine: loading -> setup -> login -> authenticated transitions work
- SetupCard creates password, LoginCard authenticates, both show appropriate errors
- FloatingLines background visible during auth screens (consistent visual)
- 4th "Einstellungen" tab visible with logout button
- All frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-exchange-api-security/07-04-SUMMARY.md`
</output>
