---
phase: 06-steuerreport-pdf-export
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - packages/backend/src/report/pdf-builder.ts
  - packages/backend/src/report/pdf-builder.test.ts
autonomous: true

must_haves:
  truths:
    - "Trade appendix table renders ALL lot consumptions (taxable + tax-free) in the PDF"
    - "Table has readable columns on A4 portrait: Symbol, Kaufdatum, Verkaufdatum, Menge, Einstandswert, Erloes, Gewinn/Verlust, Haltefrist"
    - "Table paginates correctly for 50, 100, and 200+ rows without clipping the last row on any page"
    - "Table header repeats on each new page"
    - "Tax-free rows (haltefrist met) are visually distinguishable from taxable rows"
    - "Gain/loss column uses green for positive, red for negative values"
  artifacts:
    - path: "packages/backend/src/report/pdf-builder.ts"
      provides: "buildTradeAppendix function integrated into buildPdf"
    - path: "packages/backend/src/report/pdf-builder.test.ts"
      provides: "Pagination tests for 50, 100, 200+ rows"
  key_links:
    - from: "packages/backend/src/report/pdf-builder.ts"
      to: "packages/shared/src/types/report.ts"
      via: "TradeAppendixRow type for table data"
      pattern: "TradeAppendixRow"
---

<objective>
Add the full per-trade appendix table to the PDF builder — the most complex PDF section, requiring manual coordinate-based table layout with pagination, repeating headers, alternating row colors, and color-coded gain/loss values.

Purpose: The trade appendix is the evidence document — it lists every trade for the Finanzamt and Steuerberater to verify the calculated tax amounts. Correct pagination is critical for usability with real-world data volumes (100-200+ trades per year).
Output: Complete buildPdf function now includes trade appendix; pagination tests for 50/100/200+ rows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-steuerreport-pdf-export/06-CONTEXT.md
@.planning/phases/06-steuerreport-pdf-export/06-RESEARCH.md
@.planning/phases/06-steuerreport-pdf-export/06-02-SUMMARY.md

@packages/backend/src/report/pdf-builder.ts
@packages/shared/src/types/report.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement trade appendix table with pagination in PDF builder</name>
  <files>
    packages/backend/src/report/pdf-builder.ts
  </files>
  <action>
    Add the trade appendix section to the existing `buildPdf()` function in `packages/backend/src/report/pdf-builder.ts`. This section is called after the Staking/Earn section and before page number injection.

    **Section header:**
    - Use the same colored header bar pattern: "Handelsanhang -- Alle Transaktionen"
    - Add a subtitle: "Alle Veraeusserungsgeschaefte inkl. steuerfreie Transaktionen (Haltefrist > 365 Tage)"

    **Table layout — column widths for A4 portrait (495pt content width):**
    Choose columns that fit A4 portrait. Recommended set (Claude's discretion on exact widths, but must sum to 495):
    - Symbol (~50pt)
    - Kaufdatum (~62pt) — format as dd.MM.yyyy
    - Verkaufdatum (~62pt) — format as dd.MM.yyyy
    - Menge (~50pt) — decimal, truncated
    - Einstandswert EUR (~65pt) — German EUR format
    - Erloes EUR (~65pt)
    - G/V EUR (~60pt) — green/red color coded
    - Tage (~30pt) — held days integer
    - Haltefrist (~51pt) — "Ja" (green) / "Nein"

    **Table header row:**
    - Background color: #354A5F (crypto-navy), white text, Bold font, 7pt
    - Function: `drawTableHeader(doc, columns, widths)` — reusable for repeat on new pages

    **Table data rows:**
    - Font size: 7pt, Regular
    - Alternating row background: white / #f5f5f5 (light grey)
    - Row height: ~14pt
    - Gain/loss column: green (#1a7a40) for >= 0, red (#c0392b) for < 0
    - Haltefrist "Ja" in green, "Nein" in default black
    - Tax-free rows (haltefristMet=true): optionally use a subtle left-border accent or italic to visually distinguish
    - All text: `lineBreak: false, ellipsis: true` to prevent overflow

    **Pagination logic (CRITICAL):**
    - Track `doc.y` after each row
    - If `doc.y + rowHeight > 770` (bottom threshold before footer zone), call `doc.addPage()`
    - After adding new page, redraw the table header row
    - This ensures no row is clipped at page bottom
    - Test boundary: the last row exactly at the threshold should trigger a new page (safe side)

    **Date formatting:**
    - Create helper `formatDateDe(isoDate: string): string` that converts ISO timestamps to dd.MM.yyyy
    - Handle both ISO date-only (2024-03-15) and full ISO datetime (2024-03-15T14:30:00Z)

    **Summary row at end of table:**
    - After all data rows, add a bold summary line:
      "Gesamt: [tradeCount] Transaktionen | Gewinn: [totalGains] | Verlust: [totalLosses] | Netto: [net]"

    **Integration:** Call `buildTradeAppendix(doc, data.tradeAppendix)` from buildPdf() between the earn section and the page number injection step. The function should be defined in the same file.
  </action>
  <verify>
    `npm run build -w packages/backend` succeeds.
    Manually inspect: create a quick script or test that generates a PDF with 5 sample rows and verify the output opens correctly in a PDF viewer (optional but recommended).
  </verify>
  <done>
    Trade appendix table renders in the PDF with readable columns, alternating row colors, green/red gain/loss coloring, and "Ja"/"Nein" haltefrist status. Summary row at end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pagination tests for 50, 100, 200+ rows</name>
  <files>
    packages/backend/src/report/pdf-builder.test.ts
  </files>
  <action>
    Add pagination-specific tests to the existing `pdf-builder.test.ts`:

    **Helper:** Create a `generateMockRows(count: number): TradeAppendixRow[]` function that generates N rows with varying data (mix of haltefristMet true/false, positive/negative gains, different symbols).

    **Tests:**

    1. `trade appendix with 50 rows produces multi-page PDF`:
       - Generate mockReportData with 50 TradeAppendixRow entries
       - Call buildPdf(data)
       - Assert PDF buffer is valid (starts with %PDF-)
       - Assert PDF has more than 1 page — check by counting occurrences of "/Type /Page" in the buffer string (each page object has this). With cover + 3 summaries + 50-row table, expect >= 4 pages.

    2. `trade appendix with 100 rows produces valid PDF`:
       - Same pattern with 100 rows
       - Assert valid PDF, more pages than the 50-row version

    3. `trade appendix with 200 rows produces valid PDF without error`:
       - 200 rows — stress test
       - Assert buildPdf does not throw
       - Assert valid PDF buffer

    4. `trade appendix includes summary row`:
       - Generate data with 5 rows
       - Assert buffer string contains "Gesamt" (the summary line)

    5. `trade appendix contains all rows`:
       - Generate data with 3 rows using distinct symbols ("BTC", "ETH", "SOL")
       - Assert all 3 symbols appear in the PDF buffer string

    6. `empty trade appendix does not crash`:
       - data.tradeAppendix = []
       - Assert buildPdf completes without error
  </action>
  <verify>
    `npm test -w packages/backend -- --reporter=verbose pdf-builder` all tests pass (including previous tests from 06-02).
  </verify>
  <done>
    PDF pagination is verified for 50, 100, and 200+ row trade appendices. Table headers repeat on new pages. No rows are clipped. Summary row present. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `npm run build -w packages/backend` succeeds
- `npm test -w packages/backend -- pdf-builder` all tests pass (both 06-02 and 06-03 tests)
- PDF with 200 rows generates without error
- Table header repeats on each page (verified by counting header text occurrences in buffer)
- Gain/loss colors and haltefrist status are correctly applied
</verification>

<success_criteria>
- Trade appendix table renders with 8-9 columns fitting A4 portrait
- Pagination works correctly: new page + header repeat when approaching bottom margin
- 50, 100, 200+ row counts all produce valid PDFs
- Summary row at table end shows totals
- Empty appendix handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/06-steuerreport-pdf-export/06-03-SUMMARY.md`
</output>
